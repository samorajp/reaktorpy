CREATE TABLE IF NOT EXISTS `produkty` (
  `id_produktu` INT NOT NULL AUTO_INCREMENT,
  `nazwa` VARCHAR(45) NOT NULL,
  `cena` FLOAT NOT NULL,
  PRIMARY KEY (`id_produktu`),
  UNIQUE INDEX `id_produktu_UNIQUE` (`id_produktu` ASC))
ENGINE = InnoDB
